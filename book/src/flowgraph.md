# Flowgraph

- explain that a flowgraph consists of blocks that are connected in a certain topology.
- explain how a flowgraph is constructed with the connect! macro
- include message and stream connections
- make one simple example (with messages and streams) without using the connect! macro to understand what's going on under the hood

## Flowgraph Handle
