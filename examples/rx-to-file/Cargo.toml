[package]
name = "rx-to-file"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
futuresdr = { path = "../..", features = ["soapy"] }
clap = { version = "3.0.13", features = ["derive"] }
