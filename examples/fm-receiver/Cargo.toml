[package]
name = "fm-receiver"
version = "0.1.0"
edition = "2021"

[dependencies]
futuresdr = { path = "../..", features=["soapy", "audio"] }
futuredsp = { path = "../../futuredsp", version = "0.0.3" }
