[package]
name = "msg"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = "3.1.0"
futuresdr = { path = "../.." }
log = { version = "0.4", features = ["release_max_level_off"] }

[[bin]]
name = "msg"
path = "msg.rs"
